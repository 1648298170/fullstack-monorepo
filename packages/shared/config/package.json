{
  "name": "@repo/config",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    },
    "./app-config": {
      "types": "./src/app-config/index.ts",
      "import": "./src/app-config/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint .",
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "@repo/tsconfig": "workspace:*"
  }
}
